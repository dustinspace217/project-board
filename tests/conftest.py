# conftest.py — shared pytest fixtures for the project-board test suite.
# Currently empty; fixtures will be added in later tasks (enumeration, build)
# as they need a tmp ~/Claude-like directory tree.
